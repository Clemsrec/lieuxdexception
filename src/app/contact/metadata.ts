/**
 * Métadonnées pour la page Contact
 * Générées automatiquement via système SEO universel
 */

import { generateContactMetadata } from '@/lib/smartMetadata';

export const metadata = generateContactMetadata();
